/* eslint-disable max-len */
import React from 'react';

import css from './styles.css';

const Logo = () => (
  <div className={css.logo}>
    <div className={css.falcon} />
    glassfalcon
  </div>
);

export default Logo;

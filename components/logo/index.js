/* eslint-disable max-len */
import React from 'react';
import css from './styles.css';

const Logo = () => (
  <div className={css.logo}>
    <span className={css.glass}>glass</span>
    <span className={css.falcon}>falcon</span>
    <span className={css.io}>.io</span>
  </div>
);

export default Logo;
